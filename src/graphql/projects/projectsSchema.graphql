interface IProject {
    uuid: UUID!
    name: String!
    created: DateTime!
}

type Project implements IProject {
    uuid: UUID!
    name: String!
    created: DateTime!
}

type ProjectsMutation {
    create(name: String!): ProjectDetails!
    update(uuid: UUID!, name: String): ProjectDetails!
}
