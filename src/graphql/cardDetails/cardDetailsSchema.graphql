type CardDetails implements ICard {
    uuid: UUID!
    name: String
    description: String
    story_points: Int
    created: DateTime!
    order: Int!
    assignee: Assignee
    deadline: DateTime
    tags: [Tag!]!
    fields: [CardField!]!
}