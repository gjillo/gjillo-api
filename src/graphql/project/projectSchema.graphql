interface IProjectBase {
    uuid: UUID!
    name: String!
    created: Timestamp!
}

type ProjectBase implements IProjectBase {
    uuid: UUID!
    name: String!
    created: Timestamp!
}

type Project implements IProjectBase {
    uuid: UUID!
    name: String!
    created: Timestamp!
    columns: [Column!]!
}