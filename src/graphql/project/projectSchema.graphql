interface IProjectBase {
    uuid: UUID!
    name: String!
    created: DateTime!
}

type ProjectBase implements IProjectBase {
    uuid: UUID!
    name: String!
    created: DateTime!
}

type Project implements IProjectBase {
    uuid: UUID!
    name: String!
    created: DateTime!
    columns: [Column!]!
}