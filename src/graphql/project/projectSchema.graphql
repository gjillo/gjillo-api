interface IProjectBase {
    id: Int!
    name: String!
    created: Timestamp!
}

type ProjectBase implements IProjectBase {
    id: Int!
    name: String!
    created: Timestamp!
}

type Project implements IProjectBase {
    id: Int!
    name: String!
    created: Timestamp!
    columns: [Column!]!
}